version: '3.8'

services:
  bank-core:
    build:
      context: ./backend/core
      dockerfile: Dockerfile
    container_name: bank-core
    restart: always
    env_file:
      - ./backend/core/.env
    ports:
      - "${BE_PORT:-8082}:${BE_PORT:-8082}"
    networks:
      - tirenn-net

  bank-ai:
    build:
      context: ./backend/ai-service
      dockerfile: Dockerfile
    container_name: bank-ai
    restart: always
    env_file:
      - ./backend/ai-service/.env
    ports:
      - "${AI_PORT:-8083}:${AI_PORT:-8083}"
    depends_on:
      - bank-core
    networks:
      - tirenn-net

  bank-frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: bank-frontend
    restart: always
    env_file:
      - ./frontend/.env
    ports:
      - "${FE_PORT:-7081}:${FE_PORT:-7081}"
    depends_on:
      - bank-core
      - bank-ai
    networks:
      - tirenn-net

networks:
  tirenn-net:
    external: true
